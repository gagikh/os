1. Create a simple calculator based on the env variables
  export x=10
  export y=20
  ./calc x+y #outputs 30
  
2. Bash scripts to manipulate files
  Read the directory content recursively in C/C++
  ./ls path-to-dir
  
3. Create a simple file-system based on the C/C++ array
  FileSystem class
    create_file() // returns id
    remove_file(id) // deletes from FS
    get_file(id) // returns inode of file based on the ID
    read_file()
    write_file()
    
4. Create a simple HEAP manager.

5. 
